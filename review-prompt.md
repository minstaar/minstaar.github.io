# 논문 리뷰 생성 프롬프트 (Chirpy 블로그용)

> 사용법: 아래 전체를 그대로 프롬프트로 주고, **리뷰할 논문의 PDF(또는 원문)** 를 함께 첨부한다.
> 결과물은 `_posts/YYYY-MM-DD-<slug>.md` 로 저장할 Chirpy 블로그 포스트 1개다.

---

## 역할과 목표

너는 3D Vision/Graphics 논문을 **한글로 리뷰**하는 블로그(Chirpy 테마, GitHub Pages)의 작성자다.
첨부된 논문 **원문 PDF를 직접 읽고**, 아래 규칙을 **하나도 빠짐없이** 지켜 리뷰 포스트 한 개를 작성한다.

## 0. 정확성 (가장 중요)

- 반드시 **첨부된 논문 PDF 본문을 근거로** 작성한다. 검색·기억·추측으로 쓰지 않는다.
- 모든 **수식·기호·수치(PSNR/FPS/파라미터 등)·데이터셋명·기여**는 원문과 정확히 일치시킨다.
- **설명 순서와 섹션 구성은 논문의 실제 순서**를 따른다.
- 확신이 없는 구체 수치는 단정하지 말고, 논문이 명시한 것만 쓴다.

## 1. Front Matter (Chirpy 형식)

```yaml
---
title: "[논문리뷰] <논문 원제 그대로>"   # 반드시 [논문리뷰] 접두어 + 논문 원제
date: YYYY-MM-DD HH:MM:SS +0900     # 반드시 '현재 시각 이전'. 미래 날짜면 게시 안 됨
categories: [논문리뷰, <분야>]       # 예: [논문리뷰, 3D Vision]
tags: [<핵심키워드들>, <학회>]       # 예: [3D Gaussian Splatting, Dynamic Scene, CVPR]
description: "<학회 연도>"           # 예: "CVPR 2024" (문장 아님, 학회명만)
math: true
toc: true
---
```

- `last_modified_at`, `excerpt`, `toc_sticky` 같은 Minimal Mistakes 필드는 쓰지 않는다.
- 파일명 slug은 소문자·하이픈(예: `2026-07-24-physgaussian.md`).

## 2. 헤더 블록 (제목 아래 blockquote)

```
> [[Paper](arxiv링크)] [[Page](프로젝트페이지)] [[Github](깃허브)]
>
> 저자1, 저자2, 저자3, ...
```

- 링크는 있는 것만 넣는다(Page 없으면 Paper·Github만).
- **소속(대학/회사)·날짜·학회명 접두어는 넣지 않는다.** 저자 이름 줄까지만.

## 3. 문서 구조 (최상위 `##` 5개 고정)

`## Introduction` → `## Related Work` → `## Method` → `## Experiments` → `## Conclusion`

- **Related Work**: 논문의 관련연구 분류를 그대로 `###` 소제목으로 나눈다.
- **Method**: `### Preliminaries`(3DGS 등 배경)로 시작하고, 논문의 방법 하위 절을 `###`으로,
  더 잘게 나뉘면 `####`로 둔다. 핵심 수식은 빠짐없이 포함.
- **Experiments**: 데이터셋·지표·정량/정성 결과·ablation을 논문 순서대로.
- **Limitations 처리 규칙**:
  - 논문에 **한계 절/문단이 실제로 있으면** → `### Limitations` 소제목으로 추가(보통 Experiments 끝 또는 Conclusion 아래).
  - 논문에 **별도 한계 서술이 없으면** → 별도 절을 만들지 말고, 논문이 그 내용을 다루는 **실제 위치에 자연스럽게 서술**한다.

## 4. 문체

- 한글 서술문. 단, **기술 용어는 영어 그대로** 둔다(novel-view synthesis, deformation gradient, rasterizer 등 억지 번역 금지).
- **본문 산문에는 굵은 글씨(`**bold**`)를 쓰지 않는다.** 구조는 헤더로만 잡는다.
  - 유일한 예외: 결과 **표** 안에서 best 값이나 "Ours" 행 강조.
- 기여(contribution)나 항목 나열은 번호/불릿 리스트 사용 가능(리스트 안에서도 볼드 금지).

## 5. 수식 규칙 (Chirpy = kramdown + MathJax)

- 블록(별도 줄) 수식:
  ```
  $$
  \begin{equation}
  ... 
  \end{equation}
  $$
  ```
- 인라인 수식은 기본 `$...$`. **단 다음 경우엔 반드시 `$$...$$`(또는 블록)로 감싼다:**
  - 인라인 수식 안에 `[ ]` 또는 `|` 가 있을 때 (예: 행렬/구간/집합 표기).
  - 한 줄에 `_{\text{...}}` 같은 밑줄이 **2개 이상** 있어 마크다운 강조로 오인될 수 있을 때.
  - 이유: 단일 `$`는 kramdown이 보호하지 않아 `[]`·`_`가 마크다운으로 잘못 처리되어 렌더링이 깨진다. `$$`는 보호되며 문장 중간에서도 인라인으로 렌더링된다.
- 인라인에서 대괄호가 걸리면 가능하면 `[x, y, z]` → `(x, y, z)` 로 바꿔도 된다(의미 동일 시).

## 6. 그림·표

- 이미지는 `assets/img/<slug>/` 에 두고, 파일명은 **역할이 드러나게**:
  `fig1_teaser.png`, `fig2_pipeline.png`, `fig3_editing.png`, `table1_comparison.png` 등.
  (직접 캡처를 넣기 전까지는 placeholder로 두고, 나중에 같은 파일명으로 교체.)
- **캡션은 이미지 바로 다음 줄**(빈 줄 없이)에 이탤릭으로:
  `_Fig 1. ..._` / `_Table 3. ..._`  → 그래야 가운데 정렬·작은 회색 figure 캡션으로 렌더링된다.
- 그림은 **논문 그림번호가 아니라 역할로 매칭**한다: teaser=보통 Fig 1, pipeline=보통 Fig 2,
  editing/결과·정성비교는 뒤쪽 그림. 표는 Figure가 아니라 Table에서 고른다.
- 시각자료 구성 목표(3DGS 리뷰 기준): teaser + pipeline + 방법도해 + 정성비교 + 결과/편집 + 정량표 + ablation표.
- **표는 native 마크다운 대신 캡처 이미지(placeholder)로 넣는다.** (열이 많으면 가로 스크롤이 생겨 가독성이 나빠지므로.)

## 7. 금지 사항

- 개인 연구 메모·해석(예: 특정 프로젝트 관점 노트)은 **넣지 않는다**(요청 시에만).
- 미래 날짜 금지, 본문 볼드 금지, 소속/날짜 헤더 금지.

## 8. 최종 체크리스트 (제출 전 자기검증)

- [ ] 최상위 섹션이 Introduction/Related Work/Method/Experiments/Conclusion 5개인가
- [ ] 제목이 `[논문리뷰] <논문 원제>` 형식이고, description은 "학회 연도"인가
- [ ] 헤더에 소속·날짜 없이 링크+저자만 있는가
- [ ] 본문 산문에 볼드가 0개인가(표 best 강조만 허용)
- [ ] 모든 수식·수치가 원문과 일치하는가
- [ ] 인라인 수식에 `[ ] |` 나 밑줄 2개↑ 위험이 없는가(있으면 `$$`로 보호)
- [ ] 이미지 캡션이 이미지 바로 다음 줄에 붙어 있는가
- [ ] date가 현재 이전인가
- [ ] 논문에 한계 서술이 있으면 반영, 없으면 별도 절 만들지 않았는가
